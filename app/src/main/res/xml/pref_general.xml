<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="radar_fixe_switch"
        android:summary="@string/description_radar_fixe_switch"
        android:title="@string/title_radar_fixe_switch" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="radar_feu_switch"
        android:summary="@string/description_radar_feu_switch"
        android:title="@string/title_radar_feu_switch" />

    <ListPreference
        android:defaultValue="1"
        android:entries="@array/pref_zoom_level"
        android:entryValues="@array/pref_list_values_zoom_level"
        android:key="pref_list_zoom_start"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_zoom_level" />

</PreferenceScreen>
